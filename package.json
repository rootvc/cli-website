{
  "name": "root3-cli",
  "version": "0.1.0",
  "dependencies": {
    "xterm": "^4.10.0"
  },
  "scripts": {
    "build": "npm install && cp node_modules/xterm/css/xterm.css . && cp node_modules/xterm/lib/xterm.js ."
  }
}
