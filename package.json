{
  "name": "rootvc-cli-website",
  "version": "0.1.0",
  "dependencies": {
    "xterm": "^4.10.0",
    "xterm-addon-fit": "^0.5.0",
    "xterm-addon-web-links": "^0.4.0"
  },
  "scripts": {
    "build": "npm install && cp node_modules/*/css/*.css ./css && cp node_modules/*/lib/*.js ./js"
  }
}
